// Define the "user" namespace.

// Extend the "ss" namespace with "user" entries.
if (typeof ss.vue.components['user'] === 'undefined') {
	ss.vue.components['user'] = {};
}
if (typeof ss.vue.mixins['user'] === 'undefined') {
	ss.vue.mixins['user'] = {};
}
if (typeof ss.vue.templates['user'] === 'undefined') {
	ss.vue.templates['user'] = {};
}
